import 'package:flutter/material.dart';

class CustomDiscounts extends StatelessWidget {
  const CustomDiscounts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
